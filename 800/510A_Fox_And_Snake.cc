#include <iostream>
#include <vector>
using namespace std;

int main(){
	int N, M;
	cin >> N >> M;
	string S[N][M];

	for (int i = 0; i < N; i++)
		for (int j = 0; j < M; j++)
			S[0][j] = "#"; 
}